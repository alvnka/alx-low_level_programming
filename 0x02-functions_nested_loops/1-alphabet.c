#include "main.h"
/**
 * print_alphabet - function will
 * print the alphabet
 * Return: should be the aphabet
*/

void print_alphabet(void)
{	int a;
	for (a = 'a'; a <= 'z'; a++)
	{
	_putchar(a);
	}
	_putchar('\n');
}
