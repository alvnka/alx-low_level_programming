#include "lists.h"
/**
 * free_list - function to free linked list
 * @head: function to be freed
 * Return: void
 */

void free_list(list_t *head)
{
	list_t *freeing;

	freeing = head
	while (freeing != NULL)
	{
		head = head->next;
		free(currect->str);
		free(current);
}
