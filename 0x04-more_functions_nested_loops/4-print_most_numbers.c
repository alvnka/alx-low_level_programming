#include "main.h"
/**
 * print_most_numbers - print 0-9
 * exclude 2 & 4
 * @i - hold the loop value
 * Return: numbers
 */

void print_most_numbers(void)
{
	int i = 0;

	while(i <= 9)
	{
		if ((i != 2) || (i != 4)
		{
		_putchar(i);
		}
		i++;
	}
}
